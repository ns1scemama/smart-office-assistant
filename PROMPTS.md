# PROMPTS.md: יומן העבודה מול Claude Code

התיעוד נכתב תוך כדי הבנייה, לא בדיעבד. כל רשומה: מה ביקשתי, מה חזר, ומה היה
צריך לתקן. הרשומות שמופיעות כאן הן אלה שבאמת שינו את מהלך העבודה.

---

## 1. מפרט לפני קוד

**הפרומפט:**
> קרא את שני מסמכי הקורס. הוצא מהם את רשימת דרישות ההגשה המדויקת: מה צריך
> להגיש, באיזה פורמט, קריטריוני ציון, ומה אסור לעשות. אל תתחיל לכתוב קוד.

**מה חזר:** רשימת שמונה התכונות המחייבות, הרובריקה על 100 נקודות, וארבע
הפחתות הנקודות. בנוסף הצבעה על כך שהקוד *אמור* להיות מיוצר ב־AI וש־PROMPTS.md
הוא תוצר חובה ששווה 15 נקודות.

**מה תיקנתי:** שום דבר. זה שינה את סדר העבודה. כתבתי `CONTRACT.md` ו־`SPEC.md`
לפני שורת קוד ראשונה, וזה חסך אחר כך התלבטויות על שמות שדות.

---

## 2. סקפולד מול mock בלבד

**הפרומפט:**
> צור פרויקט React + Vite עם שלושה מסכים: Upload, Dashboard, DocumentDetail.
> כל קריאות ה־HTTP ב־src/api/client.js בלבד, ו־src/api/mock.js שמחזיר את ה־JSON
> לדוגמה מ־CONTRACT.md. דגל ב־env בוחר בין mock לאמיתי. אל תקרא ל־n8n עדיין.

**מה חזר:** מבנה עובד תוך דקות, כולל RTL בעברית.

**מה תיקנתי:** הדגל היה `VITE_USE_MOCK === 'true'`, כלומר בהיעדר קובץ `.env`
האפליקציה ניסתה לפנות ל־n8n אמיתי ונפלה. הפכתי את זה ל־`!== 'false'` כך
שברירת המחדל הבטוחה היא mock. זה מנע קריאות AI מיותרות בזמן פיתוח הממשק.

---

## 3. הצומת שהחזיר סכימת דוגמה במקום השדות שלי

**הפרומפט:**
> ה־Information Extractor מחזיר `{"state":"Not found","cities":[]}` במקום שבעת
> השדות שהגדרתי. זה נראה כמו סכימת ברירת המחדל של הצומת. תקן.

**מה חזר:** אבחנה שהמבנה `attributes.attributes[]` שהעברתי לא זוהה, ולכן n8n
נפל לערך ברירת המחדל של הפרמטר.

**מה תיקנתי:** מעבר ל־`schemaType: "fromJson"` עם `jsonSchemaExample`, מחרוזת
JSON של אובייקט דוגמה. פחות תלוי בגרסה, ועבד בניסיון הראשון. את הכללים
המפורטים לכל שדה העברתי ל־`systemPromptTemplate`.

---

## 4. הענף שנעלם

**הפרומפט:**
> קובץ TXT נכנס לזרימה ולא יוצא ממנה כלום. ה־Switch מוגדר עם שני כללים
> (docx, pdf) ו־fallbackOutput: "extra", ובנתוני ההרצה יש רק שני ענפי פלט,
> שניהם ריקים.

**מה חזר:** ההסבר שה־fallback לא נוצר כפלט שלישי בפועל, ולכן הפריט פשוט נעלם
בלי שגיאה.

**מה תיקנתי:** הוספתי כלל שלישי מפורש ל־`text/plain` במקום להסתמך על מנגנון
ה־fallback. עדיף התנהגות דטרמיניסטית על פני קיצור דרך.

---

## 5. השדה ששינה שם באמצע הזרימה

**הפרומפט:**
> ה־IF בודק `$json.urgency == "High"`, החשבונית מסווגת High, ובכל זאת המייל
> יוצא בענף הרגיל. ה־IF יושב אחרי צומת Google Sheets.

**מה חזר:** צומת Sheets מחזיר את השורה עם שמות העמודות של הגיליון (`Urgency`
באות גדולה), ולכן `$json.urgency` הפך ל־undefined.

**מה תיקנתי:** כל הצמתים שאחרי Sheets מפנים ל־`$('BuildResult').item.json.<שדה>`
במקום ל־`$json`. אותו תיקון הוחל גם על שני צומתי ה־Gmail וגם על בניית התשובה
הסופית. זה הבאג שהכי קל היה לפספס, כי הזרימה הצליחה ורק הענף היה שגוי.

---

## 6. שני צומתי Respond, לא אחד

**הפרומפט:**
> בנה את Workflow A כך ש־Respond to Webhook מחובר לשני ענפי ה־IF, ובנוסף ענף
> נפרד שמחזיר 400 עם UNSUPPORTED_FILE_TYPE כשסוג הקובץ לא נתמך.

**מה חזר:** מבנה נכון, אבל `responseCode: 400` נכתב כפרמטר ברמה העליונה.

**מה תיקנתי:** הקוד החזיר 200 עם גוף שגיאה. העברתי ל־`options.responseCode`
ואז התקבל 400 אמיתי. בדיקה עם curl היא זו שגילתה את זה, לא קריאת הקוד.

---

## 7. 404 שלא היה

**הפרומפט:**
> קריאה ל־/review עם document_id שלא קיים מחזירה 200 עם גוף ריק. לפי החוזה
> היא צריכה להחזיר 404.

**מה חזר:** ההסבר שצומת Sheets מחזיר אפס פריטים כשאין התאמה, ולכן צומת ה־Respond
כלל לא רץ ו־n8n סגר את הבקשה עם 200 ריק.

**מה תיקנתי:** `alwaysOutputData` על צומת העדכון, צומת IF שבודק אם `Document ID`
חזר, וצומת Respond שני שמחזיר 404 עם `error_code: NOT_FOUND`.

---

## 8. הודעות שגיאה שעובד משרד יכול לפעול לפיהן

**הפרומפט:**
> עטוף את כל הקריאות ב־client.js כך ש־401/403 יחזירו הודעת תקלת הגדרות, 404
> יחזיר הודעה שהמסמך לא נמצא, ו־timeout אחרי 90 שניות יחזיר הודעה עם אפשרות
> לנסות שוב. אף פעם לא קוד סטטוס גולמי על המסך.

**מה חזר:** מיפוי מלא ומסודר.

**מה תיקנתי:** לא נדרש תיקון. הבדיקה עם סוד שגוי החזירה קודם "Authorization
data is wrong!", טקסט של n8n שאין לעובד משרד מה לעשות איתו. אחרי התיקון הוא
רואה משפט בעברית שמפנה אותו למנהל המערכת.

---

## מה עבד ומה לא בעבודה מול Claude Code

**עבד:** לתאר את המצב שנצפה בפועל במקום להסיק מסקנה. "ה־IF מקבל High ובכל זאת
יוצא בענף השני, וה־IF יושב אחרי Sheets" הביא לפתרון מיד. "זה לא עובד" לא היה
מביא לשום מקום.

**עבד:** לבנות מול mock קודם. כשהתחלתי לחבר ל־n8n אמיתי כבר ידעתי בוודאות
שהממשק תקין, ולכן כל תקלה הייתה תקלת אינטגרציה בלבד.

**לא עבד:** לנחש סכימות של צמתים לפי הזיכרון. שלוש טעויות רצופות
(`attributes`, `columns.schema`, `responseCode`) נפתרו רק כשקראתי את הודעת
השגיאה האמיתית מההרצה, או כשהעתקתי מבנה מזרימה קיימת שכבר עובדת באותו מופע.

**לא עבד:** curl לבדיקת תוכן עברי. הוא הרס את הקידוד והפך את הטקסט לג'יבריש,
מה שגרם לי לחשוד בבאג שלא היה קיים. מעבר לבדיקה דרך Python עם
`json.dumps(ensure_ascii=False).encode('utf-8')` פתר את זה.
